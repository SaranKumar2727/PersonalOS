import { FormEvent, useEffect, useState } from 'react'
import { ExternalLink, GitBranch, Globe2, Plus, Trash2 } from 'lucide-react'
import { API } from './api'
type Project={id:number;name:string;description:string;language:string;visibility:string;deployed_url:string;repository_url:string;readme:string;created_at:string}
const Github = GitBranch
const blank=()=>({name:'',description:'',language:'TypeScript',visibility:'Private',deployed_url:'',repository_url:'',readme:''})
export default function ProjectsPage(){const [items,setItems]=useState<Project[]>([]),[selected,setSelected]=useState<Project|null>(null),[editing,setEditing]=useState(false),[form,setForm]=useState(blank()),[error,setError]=useState('');useEffect(()=>{fetch(`${API}/projects`).then(r=>r.json()).then((data:Project[])=>{setItems(data);if(data[0])setSelected(data[0])}).catch(()=>setError('Start FastAPI to load projects.'))},[])
 const create=()=>{setForm(blank());setSelected(null);setEditing(true)};const edit=(item:Project)=>{setSelected(item);setForm(item);setEditing(true)};const save=async(e:FormEvent)=>{e.preventDefault();const r=await fetch(selected?`${API}/projects/${selected.id}`:`${API}/projects`,{method:selected?'PATCH':'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});if(r.ok){const item:Project=await r.json();setItems(current=>selected?current.map(x=>x.id===item.id?item:x):[item,...current]);setSelected(item);setEditing(false)}else setError('Could not save project.')};const remove=async()=>{if(!selected||!confirm(`Delete ${selected.name}?`))return;await fetch(`${API}/projects/${selected.id}`,{method:'DELETE'});setItems(current=>current.filter(x=>x.id!==selected.id));setSelected(null)}
 return <div className="mx-auto max-w-7xl px-5 pb-12 sm:px-9"><section className="mb-7 flex items-end justify-between"><div><p className="mb-2 text-sm font-medium text-forest">PROJECTS</p><h1 className="text-3xl font-bold tracking-tight sm:text-4xl">Build in public, or quietly.</h1><p className="mt-2 text-muted">Your personal repository space for everything you’re making.</p></div><button onClick={create} className="inline-flex items-center gap-2 rounded-xl bg-forest px-4 py-2.5 text-sm font-semibold text-white"><Plus size={17}/>New repository</button></section><section className="grid min-h-[580px] overflow-hidden rounded-2xl bg-white shadow-sm lg:grid-cols-[330px_1fr]"><aside className="border-b border-slate-100 bg-[#fafbf9] p-4 lg:border-b-0 lg:border-r"><div className="mb-4 flex items-center justify-between"><p className="text-sm font-bold">Repositories</p><span className="text-xs text-muted">{items.length}</span></div>{items.map(item=><button key={item.id} onClick={()=>{setSelected(item);setEditing(false)}} className={`mb-2 w-full rounded-xl p-3 text-left ${selected?.id===item.id?'bg-[#e8f5ed]':'hover:bg-slate-100'}`}><div className="flex items-center gap-2"><GitBranch size={15} className="text-forest"/><span className="truncate text-sm font-semibold">{item.name}</span></div><p className="mt-1 line-clamp-2 pl-5 text-xs text-muted">{item.description||'No description'}</p><div className="mt-2 flex gap-2 pl-5 text-[10px] text-muted"><span>{item.visibility}</span><span>·</span><span>{item.language}</span></div></button>)}{!items.length&&<p className="py-8 text-center text-xs text-muted">Create your first repository.</p>}</aside><main className="min-w-0 p-6 sm:p-10">{editing?<form onSubmit={save} className="mx-auto max-w-2xl space-y-4"><div className="flex items-center justify-between"><span className="text-sm font-semibold text-forest">{selected?'EDIT REPOSITORY':'NEW REPOSITORY'}</span><div className="flex gap-2"><button type="button" onClick={()=>setEditing(false)} className="rounded-lg px-3 py-2 text-xs text-muted">Cancel</button><button className="rounded-lg bg-forest px-3 py-2 text-xs font-semibold text-white">Save</button></div></div><input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="repository-name" className="w-full bg-transparent text-3xl font-bold outline-none"/><input value={form.description} onChange={e=>setForm({...form,description:e.target.value})} placeholder="Short description" className="w-full rounded-xl bg-slate-50 px-3 py-3 text-sm outline-none"/><div className="grid gap-3 sm:grid-cols-2"><input value={form.language} onChange={e=>setForm({...form,language:e.target.value})} placeholder="Language" className="rounded-xl bg-slate-50 px-3 py-3 text-sm outline-none"/><select value={form.visibility} onChange={e=>setForm({...form,visibility:e.target.value})} className="rounded-xl bg-slate-50 px-3 py-3 text-sm outline-none"><option>Private</option><option>Public</option></select></div><input type="url" value={form.repository_url} onChange={e=>setForm({...form,repository_url:e.target.value})} placeholder="GitHub repository URL (optional)" className="w-full rounded-xl bg-slate-50 px-3 py-3 text-sm outline-none"/><input type="url" value={form.deployed_url} onChange={e=>setForm({...form,deployed_url:e.target.value})} placeholder="Deployed link (optional)" className="w-full rounded-xl bg-slate-50 px-3 py-3 text-sm outline-none"/><textarea value={form.readme} onChange={e=>setForm({...form,readme:e.target.value})} placeholder="README — what is this project, and how does it work?" className="min-h-56 w-full resize-y rounded-xl bg-slate-50 p-4 text-sm leading-7 outline-none"/></form>:selected?<article className="mx-auto max-w-2xl"><div className="flex items-start justify-between"><div><div className="flex items-center gap-2"><Github size={20} className="text-muted"/><h2 className="text-3xl font-bold tracking-tight">{selected.name}</h2></div><p className="mt-3 text-muted">{selected.description}</p><div className="mt-4 flex flex-wrap gap-2"><span className="rounded-full bg-[#e8f5ed] px-2.5 py-1 text-xs font-semibold text-forest">{selected.visibility}</span><span className="rounded-full bg-slate-100 px-2.5 py-1 text-xs text-muted">{selected.language}</span></div></div><div className="flex gap-1"><button onClick={()=>edit(selected)} className="rounded-lg bg-slate-100 px-3 py-2 text-xs font-semibold">Edit</button><button onClick={remove} className="grid h-9 w-9 place-items-center rounded-lg text-muted hover:bg-red-50 hover:text-red-600"><Trash2 size={16}/></button></div></div><div className="mt-7 flex gap-2">{selected.repository_url&&<a href={selected.repository_url} target="_blank" rel="noreferrer" className="inline-flex items-center gap-2 rounded-xl bg-ink px-3 py-2 text-xs font-semibold text-white"><Github size={14}/>Repository</a>}{selected.deployed_url&&<a href={selected.deployed_url} target="_blank" rel="noreferrer" className="inline-flex items-center gap-2 rounded-xl bg-[#e8f5ed] px-3 py-2 text-xs font-semibold text-forest"><Globe2 size={14}/>Live deployment<ExternalLink size={13}/></a>}</div><div className="mt-8 border-t border-slate-100 pt-7"><div className="mb-3 flex items-center gap-2 text-sm font-bold"><GitBranch size={16}/>README</div><p className="whitespace-pre-wrap text-[15px] leading-8 text-slate-700">{selected.readme||'No README added yet. Edit this repository to document it.'}</p></div></article>:<div className="grid h-full place-items-center text-center"><div><GitBranch className="mx-auto text-forest"/><p className="mt-3 font-semibold">Create your first repository</p><p className="mt-1 text-sm text-muted">Projects you’re building will appear here.</p></div></div>}</main></section>{error&&<p className="mt-4 rounded-xl bg-amber-50 px-4 py-3 text-sm text-amber-800">{error}</p>}</div>}

import { useState } from 'react'
import { Check, GitBranch } from 'lucide-react'
const Github = GitBranch
const API='http://127.0.0.1:8000/api/v1'
export default function GitHubSettings(){const [value,setValue]=useState(()=>localStorage.getItem('personal-os-github-profile')||'');const [saved,setSaved]=useState(false);const save=async()=>{const username=value.trim().replace('https://github.com/','').replaceAll('/','');const r=await fetch(`${API}/auth/github-profile`,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({username})});if(r.ok){localStorage.setItem('personal-os-github-profile',username);setValue(username);setSaved(true);setTimeout(()=>setSaved(false),1800)}};return <section className="rounded-2xl bg-white p-6 shadow-sm"><div className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-xl bg-[#e8f5ed] text-forest"><Github size={18}/></span><div><h2 className="font-bold">GitHub profile</h2><p className="mt-1 text-xs text-muted">Use this to mirror your public repositories in Projects.</p></div></div><div className="mt-5 flex gap-2"><input value={value} onChange={e=>setValue(e.target.value)} placeholder="github.com/your-username" className="min-w-0 flex-1 rounded-xl bg-slate-50 px-3 py-3 text-sm outline-none"/><button onClick={save} className="rounded-xl bg-forest px-4 py-2 text-sm font-semibold text-white">{saved?<Check size={16}/>: 'Save'}</button></div></section>}
